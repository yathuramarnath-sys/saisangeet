# Apps Workspace

This folder holds the client applications for the restaurant platform.

## Planned apps

- `owner-web` for owner and admin workflows
- `operations-pos` for cashier and billing operations
- `waiter-mobile` for waiter and captain workflows
- `kitchen-display` for kitchen queue operations

These apps will share the backend API and common domain rules.
